name: 'RSVP List'
description: 'Allow user to RSVP for Events.'
package: 'RSVP List'
type: module
version: 1.0
core_version_requirement: ">=8"

dependencies:
  - drupal:block

configure: rsvplist.admin_settings # Si te algo en la configuració, tot i que falta fer la ruta i el form...
